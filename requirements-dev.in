# Development/test dependencies (unpinned).
# Compile to requirements-dev.txt with:
#   pip-compile requirements-dev.in -o requirements-dev.txt
-r requirements.in
pytest>=8.0
ruff>=0.4
pip-tools>=7.0
