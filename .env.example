# ============================================================
# Archivo de variables de entorno para generateArticle.py
# Copia este fichero como .env y rellena los valores reales:
#   cp .env.example .env
# ============================================================

# -------- MongoDB --------
# Opción A – MongoDB Atlas (recomendado):
# Sustituye <db_password> por la contraseña real del usuario ex_dbuser.
MONGODB_URI=mongodb+srv://ex_dbuser:<db_password>@cluster0.9kjmkdg.mongodb.net/?appName=Cluster0
DB_NAME=blogdb

# Opción B – MongoDB local con Docker Compose (desarrollo local):
# Credenciales usadas por docker-compose.yml para crear el usuario root:
# MONGO_ROOT_USER=admin
# MONGO_ROOT_PASS=admin1234
# MONGODB_URI=mongodb://admin:admin1234@localhost:27017/blogdb?authSource=admin

# -------- Colecciones de MongoDB --------
CATEGORY_COLL=categories
TAGS_COLL=tags
USERS_COLL=users
ARTICLES_COLL=articles

# -------- OpenAI --------
OPENAIAPIKEY=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OPENAI_MODEL=gpt-4o

# -------- Sitio web (URL base sin barra final) --------
SITE=https://tusitio.com

# -------- Autor --------
AUTHOR_USERNAME=adminUser

# -------- Notificaciones por email (SMTP) --------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_contraseña_de_aplicacion
FROM_EMAIL=tu_correo@gmail.com
NOTIFY_EMAIL=tu_correo@gmail.com

# -------- Opciones de comportamiento --------
# Enviar emails detallados (true/false)
NOTIFY_VERBOSE=true
# Limitar a 1 artículo por semana (true/false)
LIMIT_PUBLICATION=true
# Enviar por email el prompt antes de llamar a OpenAI (true/false)
SEND_PROMPT_EMAIL=false
