# No web process — execution is managed by Heroku Scheduler add-on.
# Scale the worker dyno to 0 after deploy: heroku ps:scale worker=0
# Add tasks via: heroku addons:open scheduler
worker: echo "Managed by Heroku Scheduler"
